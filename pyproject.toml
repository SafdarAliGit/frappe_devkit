[project]
name = "frappe_devkit"
version = "0.0.1"
description = "Frappe development utilities"
authors = [
    {name = "Safdar Ali", email = "your@email.com"}
]
dependencies = []

[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"